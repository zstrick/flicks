<% flash.each do |key, value| %>
  <% if key == "notice" %>
    <%= content_tag(:p, value, :class => "alert alert-success") %>
  <% else %>
    <%= content_tag(:p, value, :class => "alert alert-danger") %>
  <% end %>
<% end %>
